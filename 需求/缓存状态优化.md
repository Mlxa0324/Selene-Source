lib\widgets\video_player_widget.dart

698行 
if (_isBuffering || _isLoadingVideo)
                  const Center(
                    child: CircularProgressIndicator(
                      color: Colors.white,
                    ),
                  ),


这段代码我想要在按钮组不显示的时候，才显示加载状态